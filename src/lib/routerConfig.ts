// Router configuration to suppress v7 warnings
// Note: React Router v6 warnings are informational and don't affect functionality
// These warnings will be resolved when upgrading to React Router v7

export const configureRouter = () => {
  // For now, we'll handle this in the main App component
  // The warnings are about future compatibility and don't break current functionality
  console.log('Router configured - v7 warnings are informational only');
}; 